#include "array_functions.h"

void game() 
{

}

int main() 
{
    /*map a;
    scanf("%d %d", &a.n, &a.m);
    a.matrix = (elem**)(malloc(sizeof(elem*) * a.n));
    for (int i = 0; i < a.n; i++) {
        a.matrix[i] = (elem*)malloc(sizeof(elem*) * a.m);
        for (int j = 0; j < a.m; j++) {
            a.matrix[i][j].is_live = 0;
            printf("%d ", a.matrix[i][j].is_live);
        }
        printf("\n");
    }
    for (int i = 0; i < a.n; i++) {
        for (int j = 0; j < a.m; j++) {
            printf("%d ", a.matrix[i][j]);
        }
        printf("\n");
    }*/
    return 0;
}